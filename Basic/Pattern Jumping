class Solution {
    static String canJump(Long N) {
        
        if (N <= 0) return "False";
        if ((N & (N - 1)) == 0) {
            return "True";
        } else {
            return "False";
        }
    }
}
