class Solution {
    static int findPosition(int n) {
        if (n == 0 || (n & (n - 1)) != 0) {
            return -1;
        }
        
        int count = 0;
        while (n > 0) {
            count++;
            n = n >> 1;
        }
        return count;
    }
}
