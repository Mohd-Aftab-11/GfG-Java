import java.util.*;

class Solution {
    public boolean checkEdge(ArrayList<ArrayList<Integer>> adj, int u, int v) {
        for (int neighbor : adj.get(u)) {
            if (neighbor == v) {
                return true;
            }
        }
        return false;
    }
}
